import 'package:flutter/material.dart';

class RegistroPontoScreen extends StatelessWidget {
  const RegistroPontoScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Registrar Ponto')),
      body: const Center(child: Text('Tela de registro de ponto (implemente lógica real)')),
    );
  }
}
